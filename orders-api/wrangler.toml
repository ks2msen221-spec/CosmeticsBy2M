name = "orders-api"
main = "src/index.ts"
compatibility_date = "2024-05-29"

# SUPABASE_URL et SUPABASE_SERVICE_ROLE_KEY sont configurées exclusivement via
# `wrangler secret put` ou le Cloudflare Dashboard > Settings > Variables and Secrets.
# Ne jamais les écrire ici en clair, sous peine d'écraser la config au prochain deploy.